[project]
name = "uml-now-mcp"
version = "0.1.0"
description = "MCP Server Implementation of the UML-NOW API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bs4>=0.0.2",
    "fastmcp>=3.1.1",
    "lxml>=6.0.2",
    "playwright>=1.58.0",
    "requests>=2.33.0",
]
